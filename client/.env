## Frontend environment variables (Vite)
## Create `client/.env` locally, and set the same variable in Vercel.

# Backend origin (NO trailing /api). Example:
# VITE_API_BASE_URL=https://your-backend.up.railway.app
VITE_API_BASE_URL=https://gaowale.vercel.app

